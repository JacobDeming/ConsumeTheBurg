<div class="container">
	<div class="row">
		<div class="col-xs-12 text-center" id="top">
			<h1>GET SOME BURGS</h1>
		</div>
		<div class="col-xs-6 text-center" class="task">
			<div class="row">
			  {{#each x}}
			  	{{#unless this.devoured}}
				<div class="col-xs-9 text-center">
					<pre>{{this.id}}. {{this.burger_name}}
					</pre>
				</div>
				<div class="col-xs-3 text-center">
					<form method="POST" action="/update?_method=PUT" class="button-size">
						<button type="submit" name="id" value={{this.id}} class="btn btn-default">EAT THAT BURG!</button>
					</form>
				</div>
				{{/unless}}
			  {{/each}}
			</div>
		</div>
		<div class="col-xs-6 text-center" class="task">
			  {{#each x}}
			  	{{#if this.devoured}}
			  		<input class="form-control" type="text" placeholder="{{this.id}} . {{this.burger_name}}" readonly>
				{{/if}}
			  {{/each}}
		</div>
	</div>
</div>

<div class="container">
	<div class="row">
		<div class="col-xs-12 text-center enter_section">
			<form action="/create" method="POST">
				<input type="text" class="form-control" name="burger" value="" id="enter_text">
				<button type="submit" class="btn btn-primary" id="text-enter-button">Submit</button>
			</form>
			<p>Enter a burger you would like to eat!</p>
		</div>
	</div>
</div>